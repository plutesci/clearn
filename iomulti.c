#include <stdio.h>
//Enter multiple values to be displayed

int main()

{

	int a;
	float b;

	printf("Enter interger and then a float: ");

	// Taken multiple inputs float and iterger
	scanf("%d%f", &a, &b);

	printf("You have entered %d and %f\n", a, b);
	return 0;

}
